# Archive: Phase 2 device frame

Physical frame helpers in `inkcut/device/frame.py`; `feed_sense` (`positive`/`negative`, default `negative` for stock BL+y); config still stores `origin_position` with `origin_corner` naming in new APIs.
